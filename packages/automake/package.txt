url: automake-1.14.1.tar.xz
version: 1.14.1
extract-dir: automake-1.14.1
configure: "${EXTRACT_DIR}"/configure --prefix="${PREFIX}"
build: make -j$NJOBS
install: make install
depends:
    autoconf >= 2.65

detect: automake --version|head -n1|grep -Eo "automake \(GNU automake\) [^ ]+"|sed "s/automake (GNU automake) //"
