url: qemu-2.8.0.tar.xz
version: 2.8.0
extract-dir: qemu-2.8.0
Darwin/configure:
    "${EXTRACT_DIR}"/configure \
        --prefix="${PREFIX}" \
        --extra-cflags="-DNCURSES_WIDECHAR=1" \
        --disable-bsd-user \
        --disable-guest-agent \
        --enable-modules --enable-curses \
        --enable-cocoa \
        --disable-vde \
        --disable-strip \
        --enable-cocoa

configure:
    "${EXTRACT_DIR}"/configure \
        --prefix="${PREFIX}" \
        --disable-bsd-user \
        --disable-guest-agent \
        --enable-modules \
        --enable-curses \
        --disable-vde

build: make -j$NJOBS
install: make install
depends:
    pkg-config
    libtool
    jpeg
    gnutls
    glib
    pixman
    ncurses

