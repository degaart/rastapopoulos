diff -rupN mtools-4.0.18.orig/sysincludes.h mtools-4.0.18/sysincludes.h
--- mtools-4.0.18.orig/sysincludes.h	2013-01-08 01:45:56.000000000 +0300
+++ mtools-4.0.18/sysincludes.h	2015-11-01 12:45:14.000000000 +0300
@@ -103,13 +103,13 @@ typedef void *caddr_t;
 # define PACKED __attribute__ ((packed))
 # if __GNUC__ == 2 && __GNUC_MINOR__ > 6 || __GNUC__ >= 3
 /* gcc 2.6.3 doesn't have "unused" */		/* mool */
-#  define UNUSED(x) x __attribute__ ((unused));x
-#  define UNUSEDP __attribute__ ((unused))
+#  define UNUSED(x) x
+#  define UNUSEDP
 # else
 #  define UNUSED(x) x
 #  define UNUSEDP /* */
 # endif
-# define NORETURN __attribute__ ((noreturn))
+# define NORETURN
 #else
 # define UNUSED(x) x
 #  define UNUSEDP /* */
