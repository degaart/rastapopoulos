url: gnutls-3.2.9.tar.xz
version: 3.2.9
extract-dir: gnutls-3.2.9
configure: "${EXTRACT_DIR}"/configure --prefix="${PREFIX}" --disable-dependency-tracking --disable-silent-rules --disable-heartbeat-support --sysconfdir="$PREFIX/etc" --with-default-trust-store="$PREFIX/etc/openssl/cert.pem" --disable-hardware-acceleration --disable-static
build: make -j$NJOBS
install: make install
depends:
    pkg-config
    gmp
    nettle

